(ns phel-aa\image\tests
  (:require phel\str)
  (:require phel\test :refer [deftest is])
  (:require phel-aa\image-reader)
  (:require phel-aa\similar-chars))

#(deftest php-logo-image-block-point-to-hist
#  (let [filename "tests/PHP-logo.png"
#        image (image-reader/create-image filename)
#        char-cells (image-reader/get-char-cells filename)
#        dict (similar-chars/get-dict)
#        string (image-reader/convert dict image char-cells)]
#    (is (str/starts-with? string "```"))))
